# Poppler C++ headers (Ubuntu/jammy poppler 22.02)
# Runtime still needs system libpoppler-cpp.so.0 (package: libpoppler-cpp0v5).
# Dev package not required at build time because headers are vendored here.
